services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: poryforum
      POSTGRES_PASSWORD: poryforum
      POSTGRES_DB: poryforum
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: